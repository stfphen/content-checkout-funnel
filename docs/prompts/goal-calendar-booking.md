# /goal Prompt — Native Booking Calendar Add-on (v2, repo-verified)

v2 of this prompt, corrected against `main@32c9f73` on 2026-07-04 — every path, pattern,
migration number, and known-issue reference below was verified against the codebase and brain.
See `docs/prompts/goal-calendar-booking-review.md` for the claim-by-claim review.

Paste everything below the line as the prompt.

---

/goal Build a native, tenant-aware Booking Calendar module (Cal.com-style) into this repo: prospects pick a date + time slot to book consultations during checkout and from funnel pages; owners manage it from a new Admin tab and sync to Google/Outlook. ULTRAPLAN first — read `brain/00-Index/00-Home.md`, `12-Repo-Structure`, `13-Data-Model`, `14-Routes-Map`, `53-Known-Issues`, run `git status --short --branch`, then present a full written plan (files, migrations, routes, risks, test list) and WAIT for my approval before writing code. Work on a small branch; small commits; never touch other worktrees.

PREFLIGHT (resolve in the plan, before any code): (1) CLAUDE.md's standing priority is "stabilize before building features" and `31-Current-Priorities` puts deploy + C1/H3/H4 security fixes first — state in the plan whether those are done or that I am consciously overriding the sequence, and get my explicit sign-off on that line. (2) H4 (`53-Known-Issues`) is an OPEN bug in the unsubscribe/suppression path, not just a rule: null-tenant suppressions match every team. The booking-confirmation email path must be team/tenant-scoped and must not extend H4's blast radius — propose either fixing H4 first or a scoped workaround. (3) Booking confirmations are TRANSACTIONAL email (individual, user-initiated), distinct from the gated "outreach at volume" item in `34-Do-Not-Start-Yet` — no sequences or bulk sends from the booking path. (4) The repo has NO timezone library (native `Date` only) — decide in the plan: add `luxon` for slot math, or hand-roll UTC math with `Intl.DateTimeFormat`; don't defer this to mid-build.

ARCHITECTURE (mock-first, no vendor lock): build the scheduling engine in-repo — no external booking SaaS. Provider sync goes behind a `lib/scheduling/providers/` adapter interface (`google.js`, `microsoft.js`, `ics.js`) with a `mock.js` default so everything works with zero credentials — mirror the telephony pattern exactly (`lib/telephony/index.js`: `getProvider()` selected by env var, shared interface documented in a `types.js`, routes import only from the index, mock default; `lib/media/index.js` mirrors it too). Note there is NO existing OAuth flow anywhere in the repo (Google exists only as Places/YouTube API keys; Microsoft not at all) — real OAuth (Google Calendar API + Microsoft Graph) is net-new work; keep it isolated behind the adapter so Phases 1–3 ship on `mock.js`/`ics.js` alone and OAuth can land last without blocking. There is also NO token-encryption utility in the repo (`crypto` is only used for `randomUUID`; `bcryptjs` only for passwords) — build a small `lib/crypto.js` (AES-256-GCM, key from a new env var, e.g. `CREDENTIALS_ENCRYPTION_KEY`) for stored provider tokens. Add all new env vars to `.env.example` + the tables in brain `43-Environment-Variables`. Never hardcode one tenant or one service path.

PHASE 1 — SCAFFOLD: migration `008_scheduling.sql` (verify 008 is still the next free number on disk first — `53-Known-Issues` separately proposes a `007_performance_indexes.sql`, so there is collision risk): tables `event_types`, `availability_rules` (weekday windows, timezone, min-notice, max-horizon), `availability_overrides` (date blocks), `bookings` (lead_id nullable, name, email, phone, start/end UTC, status pending|confirmed|cancelled|rescheduled, provider_event_id, source page, notes), `calendar_connections` (provider, encrypted tokens, sync state). EVERY table gets `team_id NOT NULL` as the isolation key plus `tenant_id` where per-tenant scoping applies (that's the house convention per migration 003 and `media_assets` — `tenant_id` alone is NOT enough), snake_case naming, and a `(team_id, created_at desc)` composite index. Follow the dual-store pattern with BOTH halves: the SQL migration AND (a) DDL added to `ensureSchema()` in `lib/store.js`, (b) new array keys in the file-store shape in `readFileStore()`, (c) CRUD functions that branch pg/file exactly like the `media_assets` functions do. Keep pg-and-file-store parity — the open `createLead` dedupe-parity bug (`53-Known-Issues`, HIGH) is the cautionary tale: never implement a behavior in only one branch. Core lib `lib/scheduling/`: slot computation (rules − overrides − existing bookings − provider busy-times, all TZ-safe UTC math per the preflight decision), booking create/cancel/reschedule with idempotency keys and atomic claim semantics (learn from the Stripe-webhook-idempotency and outreach double-send issues in `53-Known-Issues` — no read-then-write races on booking status).

PHASE 2 — BUILD: split APIs by the house namespace convention — PUBLIC: `app/api/scheduling/` (`GET slots`, `POST bookings`), tenant-scoped, validated, rate-limited via `consumeRateLimit` + `clientIpFromRequest` from `lib/rateLimit.js` (note: in-process fixed-window, single-node — fine for now), and hardened per the PLANNED-SURFACE checklist in `53-Known-Issues` (team-scoped IDOR checks, input whitelisting, SSRF guard on any server-side URL fetch). ADMIN: `app/api/admin/scheduling/` behind `requireRole` from `lib/permissions.js` — CRUD for event types, availability, bookings list, connection OAuth start/callback. Audit-log all admin mutations via `logAudit` from `lib/audit.js`, but guard it: `logAudit` is pg-only and throws without `DATABASE_URL`, so wrap or skip in file-store mode or the module breaks in zero-credential mock mode. Frontend: a reusable `<BookingWidget/>` component (month/date grid → timezone-aware slot list → confirm form) modeled on `components/funding/FundingSurveyWidget.jsx` (tenant prop, self-contained, own `.module.css`, embeddable anywhere via props). Theming: the PUBLIC widget follows the funnel's light, tenant-branded design (per DESIGN.md — the public funnel is deliberately light for conversion); dark-first styling applies only inside the admin shell (`16-Design-System`). Admin: new "Calendar" tab — three touchpoints: add to `navItems` in `components/admin/AdminTabbedShell.jsx`, add `"calendar"` to the `visibleTabs` computation in `app/admin/page.jsx`, add an `<AdminTabPanel tabId="calendar">` (don't break existing navigation — product priority #1; the mobile bottom bar already auto-overflows past 5 tabs). Sub-views: Bookings (list/filter/cancel/reschedule), Event Types, Availability editor, Connections (connect Google/Outlook, show sync status).

PHASE 3 — INTEGRATE: mount the widget in checkout as an optional "book a consultation" step tied to service packages — register it as a section in the `SECTION_COMPONENTS` registry + `design.sectionOrder` in `components/FunnelPage.jsx` (the way `fundingPromo` slots in), and note the existing `selectedPackage.bookingLink` redirect branch there as the seam to extend. Add the standalone booking page at `/t/[slug]/book/[eventType]` (the tenant page param is `[slug]`, and `app/t/[slug]/page.jsx` is currently the only page under `app/t/` — this is the first nested route). Surface booking CTAs where funnels/outreach need them. On booking: upsert the lead via `sanitizePublicLeadInput` (`lib/leadUtils.js`) → `createLead` (`lib/store.js`) exactly as `/api/checkout` does (don't rely on `skippedDuplicate` in pg mode until the dedupe-parity bug is fixed); create the provider calendar event through the adapter; send the confirmation via `sendResendEmail` (`lib/integrations/resend.js`) with a team/tenant-scoped suppression check modeled on `canSendQueueItem`/`findSuppression` (`lib/outreachSequence.js`) — see the PREFLIGHT H4 constraint. Seed demo event types + availability in `scripts/seed-tenants.js` (it's idempotent and supports `--only <slug,…>` — keep it that way).

PHASE 4 — TEST & VALIDATE: `node --test` unit tests for slot math (TZ edges, DST, buffers, min-notice, overrides), double-booking/idempotency races, pg-vs-file-store parity of every new store function, tenant/team isolation, API auth; keep the CURRENT full suite green (run `npm test` first to record the baseline count — don't trust a remembered number). Run `npm test` and `npm run build`; report exact commands, results, and full file-change list. Manually verify admin nav + checkout still work. Finish by updating the brain: `51-Timeline`, `52-Decision-Log` (the preflight override decision if taken), `13-Data-Model`, `14-Routes-Map`, `43-Environment-Variables`, and a new module note `20-Modules/2E-Scheduling.md` (2E is the next free hex ID) created from `99-Templates/Module-Note-Template` and linked from `20-Modules-MOC`; bump `updated:` dates. Do not deploy; stop after green tests + brain update and summarize.
